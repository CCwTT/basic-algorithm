1、query和update的时候，一定不会到n，因为需要右边界减一，但是写update(1,1,sgt.n,...)不会有问题，
然而如果要build,最好写build(1,1,n-1),此时update也要相应写upate(1,1,sgt.n-1,...).
如果build写(1,1,n)，注意右侧边界特判或disc.disc增加右侧哨兵。
2、up时注意叶子节点特判，如果不特判，需要开8倍空间。
3、Event(ll x=0,ll y1=0,ll y2=0,ll v=0):x(x),y1(y1),y2(y2),v(v){}


防止时间久了，基本的定义忘了：
times的含义是这个段上完整覆盖的层数。
cover的含义是这个段上有覆盖的总长度。
